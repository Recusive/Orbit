/**
 * Terminal store - Terminal session state
 */

export { useTerminalStore } from './terminal-store';
export type {
  TerminalOutput,
  DetectedCommand,
  ForegroundProcess,
  TerminalSession,
  TerminalPreferences,
  TerminalState,
} from './terminal-store';
