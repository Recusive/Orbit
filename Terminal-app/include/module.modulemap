// This makes libghostty available to the XCode build for the macOS app.
// We append "Kit" to it not to be cute, but because targets have to have
// unique names and we use OrbitTerminal for other things.
module OrbitTerminalKit {
    umbrella header "ghostty.h"
    export *
}
