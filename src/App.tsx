import type { FC } from 'react';

import { RootLayout } from '@/components/layout';

const App: FC = () => {
  return (
    <div className="dark h-full">
      <RootLayout />
    </div>
  );
};

export default App;
