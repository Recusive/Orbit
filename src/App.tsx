import type { FC } from 'react';

import { RootLayout } from '@/components/layout';

const App: FC = () => {
  return (
    <div className="dark">
      <RootLayout />
    </div>
  );
};

export default App;
