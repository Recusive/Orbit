# docs

> **Path:** `Agent-backend/docs/`

## Purpose

Internal documentation and planning documents. Contains implementation plans and architecture decisions.

## Usage Status

| Product             | Status      | Notes                                                                                 |
| ------------------- | ----------- | ------------------------------------------------------------------------------------- |
| Orbit Desktop (SDK) | `reference` | Planning documents may contain architecture decisions relevant to Orbit's integration |
| Orbit CLI           | `reference` | Same                                                                                  |

## Directories

| Directory    | Purpose                   |
| ------------ | ------------------------- |
| `plans/`     | Implementation plans      |
| `plans/git/` | Git-related feature plans |

## Notes

- **Separate from `packages/docs/`** — this `docs/` directory contains internal dev documentation, while `packages/docs/` is the public Mintlify documentation site.
- **`docs/plans/OPENCODE-MIGRATION-PLAN.md`** exists in the git status as untracked — likely the Orbit migration plan.
