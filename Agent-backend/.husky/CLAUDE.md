# .husky

> **Path:** `Agent-backend/.husky/`

## Purpose

Git hook scripts managed by Husky. Contains a `pre-push` hook that runs before pushing to remote.

## Usage Status

| Product             | Status     | Notes              |
| ------------------- | ---------- | ------------------ |
| Orbit Desktop (SDK) | `not used` | Upstream git hooks |
| Orbit CLI           | `not used` | Same               |

## Files

| File       | Purpose                  |
| ---------- | ------------------------ |
| `pre-push` | Pre-push validation hook |
| `_/`       | Husky internals          |
