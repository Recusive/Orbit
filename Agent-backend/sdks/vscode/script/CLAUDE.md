# script

> **Path:** `Agent-backend/sdks/vscode/script/`

## Purpose

Build and release scripts for the VS Code extension, with subdirectories for `publish` and `release` workflows.

## Usage Status

| Product             | Status     | Notes                    |
| ------------------- | ---------- | ------------------------ |
| Orbit Desktop (SDK) | `active`   | Extension build pipeline |
| Orbit CLI           | `not used` | VS Code extension only   |
