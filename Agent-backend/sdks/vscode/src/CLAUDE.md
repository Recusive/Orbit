# src

> **Path:** `Agent-backend/sdks/vscode/src/`

## Purpose

VS Code extension source containing the main `extension.ts` entry point that registers commands, activates the extension, and bridges VS Code with the agent backend.

## Usage Status

| Product             | Status     | Notes                         |
| ------------------- | ---------- | ----------------------------- |
| Orbit Desktop (SDK) | `active`   | VS Code extension entry point |
| Orbit CLI           | `not used` | VS Code extension only        |
