# script

> **Path:** `Agent-backend/packages/sdk/js/script/`

## Purpose

SDK build and publish scripts. Contains `build.ts` (compiles TypeScript SDK to distributable output) and `publish.ts` (publishes the SDK package to npm registry).

## Usage Status

| Product             | Status   | Notes              |
| ------------------- | -------- | ------------------ |
| Orbit Desktop (SDK) | `active` | SDK build pipeline |
| Orbit CLI           | `active` | SDK build pipeline |
