# storybook

> **Path:** `Agent-backend/packages/ui/src/storybook/`

## Purpose

Storybook utilities for component development and testing. Contains `fixtures.ts` (mock data and test fixtures for stories) and `scaffold.tsx` (story wrapper/decorator component for consistent story rendering).

## Usage Status

| Product             | Status     | Notes                                             |
| ------------------- | ---------- | ------------------------------------------------- |
| Orbit Desktop (SDK) | `not used` | Desktop has its own test setup                    |
| Orbit CLI           | `rebuild`  | Fixture patterns may inform CLI component testing |
