# hooks

> **Path:** `Agent-backend/packages/ui/src/hooks/`

## Purpose

SolidJS reactive hooks for the UI package. Contains `create-auto-scroll.tsx` (auto-scrolling behavior for chat/log views), `use-filtered-list.tsx` (filtered/searchable list logic), and `index.ts` (barrel export).

## Usage Status

| Product             | Status     | Notes                                             |
| ------------------- | ---------- | ------------------------------------------------- |
| Orbit Desktop (SDK) | `not used` | Desktop uses React hooks                          |
| Orbit CLI           | `rebuild`  | Auto-scroll and filtering logic patterns to study |
