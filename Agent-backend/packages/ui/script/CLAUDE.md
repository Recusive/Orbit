# script

> **Path:** `Agent-backend/packages/ui/script/`

## Purpose

UI package build and generation scripts. Contains `tailwind.ts` (Tailwind CSS configuration generator) and `colors.txt` (color palette reference used during generation).

## Usage Status

| Product             | Status     | Notes                                        |
| ------------------- | ---------- | -------------------------------------------- |
| Orbit Desktop (SDK) | `not used` | Desktop has its own styling                  |
| Orbit CLI           | `rebuild`  | Study Tailwind generation for CLI UI rebuild |
