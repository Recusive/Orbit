# src

> **Path:** `Agent-backend/packages/plugin/src/`

## Purpose

Plugin SDK source providing the public API for building plugins. Contains the entry point (`index.ts`), shell integration (`shell.ts`), tool definitions (`tool.ts`), and example plugin (`example.ts`).

## Usage Status

| Product             | Status   | Notes                                 |
| ------------------- | -------- | ------------------------------------- |
| Orbit Desktop (SDK) | `active` | Plugin SDK for third-party extensions |
| Orbit CLI           | `active` | Plugin SDK for third-party extensions |
