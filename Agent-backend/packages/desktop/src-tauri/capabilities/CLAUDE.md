# capabilities

> **Path:** `Agent-backend/packages/desktop/src-tauri/capabilities/`

## Purpose

Tauri v2 permission capability definitions. Contains `default.json` which declares the set of Tauri API permissions granted to the desktop app's webview windows.

## Usage Status

| Product             | Status      | Notes                              |
| ------------------- | ----------- | ---------------------------------- |
| Orbit Desktop (SDK) | `reference` | Study Tauri ACL/permission model   |
| Orbit CLI           | `not used`  | CLI has no Tauri capability system |
