# beta

> **Path:** `Agent-backend/packages/desktop/src-tauri/icons/beta/`

## Purpose

Beta release channel app icons for the Tauri desktop build. Contains all platform-specific icon formats (`.icns`, `.ico`, `.png` at multiple sizes, Windows Store `Square*.png`) plus `android/` and `ios/` subdirectories.

## Usage Status

| Product             | Status      | Notes                 |
| ------------------- | ----------- | --------------------- |
| Orbit Desktop (SDK) | `reference` | Beta channel branding |
| Orbit CLI           | `not used`  | CLI has no app icons  |
