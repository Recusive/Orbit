# server

> **Path:** `Agent-backend/packages/app/src/components/server/`

See parent CLAUDE.md at `packages/app/CLAUDE.md` for detailed documentation of this directory.

## Purpose

Server-related UI components. Contains a single component for rendering server connection rows in the server selection dialog.

## Usage Status

| Product             | Status      | Notes                                                                     |
| ------------------- | ----------- | ------------------------------------------------------------------------- |
| Orbit Desktop (SDK) | `not used`  | Orbit connects to a single embedded server, not multiple                  |
| Orbit CLI           | `reference` | Multi-server connection UI could be useful if CLI supports remote servers |
