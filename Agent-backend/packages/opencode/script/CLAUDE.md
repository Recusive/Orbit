# script

> **Path:** `Agent-backend/packages/opencode/script/`

## Purpose

Build, development, and utility scripts including the main build script (`build.ts`), migration checker, schema generator, postinstall hook, and E2E seed script.

## Usage Status

| Product             | Status   | Notes                                          |
| ------------------- | -------- | ---------------------------------------------- |
| Orbit Desktop (SDK) | `active` | Build scripts used for SDK sidecar compilation |
| Orbit CLI           | `active` | Build and publish scripts for CLI releases     |
