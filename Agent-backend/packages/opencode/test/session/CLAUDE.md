# session

> **Path:** `Agent-backend/packages/opencode/test/session/`

## Purpose

Tests for session management including compaction, system instructions, LLM integration, message v2 format, prompt construction, retry logic, revert-compact, session lifecycle, and structured output.

## Usage Status

| Product             | Status   | Notes                       |
| ------------------- | -------- | --------------------------- |
| Orbit Desktop (SDK) | `active` | Validates session lifecycle |
| Orbit CLI           | `active` | Validates session lifecycle |
