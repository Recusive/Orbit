# fixtures

> **Path:** `Agent-backend/packages/opencode/test/config/fixtures/`

## Purpose

Markdown fixture files for config parsing tests, covering frontmatter extraction, empty frontmatter, no-frontmatter, markdown headers, and unusual model ID formats.

## Usage Status

| Product             | Status   | Notes                            |
| ------------------- | -------- | -------------------------------- |
| Orbit Desktop (SDK) | `active` | Test fixtures for config parsing |
| Orbit CLI           | `active` | Test fixtures for config parsing |
