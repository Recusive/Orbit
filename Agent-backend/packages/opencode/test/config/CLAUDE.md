# config

> **Path:** `Agent-backend/packages/opencode/test/config/`

## Purpose

Tests for configuration loading and parsing including agent color assignment, markdown config files, TUI config, and general config validation with fixture files.

## Usage Status

| Product             | Status   | Notes                    |
| ------------------- | -------- | ------------------------ |
| Orbit Desktop (SDK) | `active` | Validates config parsing |
| Orbit CLI           | `active` | Validates config parsing |
