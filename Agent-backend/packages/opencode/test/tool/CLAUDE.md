# tool

> **Path:** `Agent-backend/packages/opencode/test/tool/`

## Purpose

Tests for built-in tools including apply_patch, bash, edit, external-directory access, grep, question, read, write, webfetch, skill tool, tool registry, and output truncation, with snapshot files and fixture data.

## Usage Status

| Product             | Status   | Notes                          |
| ------------------- | -------- | ------------------------------ |
| Orbit Desktop (SDK) | `active` | Validates tool implementations |
| Orbit CLI           | `active` | Validates tool implementations |
