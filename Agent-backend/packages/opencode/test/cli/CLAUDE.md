# cli

> **Path:** `Agent-backend/packages/opencode/test/cli/`

## Purpose

Tests for CLI-specific functionality including GitHub action integration, remote URL parsing, import commands, plugin auth picker, and TUI rendering.

## Usage Status

| Product             | Status   | Notes                                |
| ------------------- | -------- | ------------------------------------ |
| Orbit Desktop (SDK) | `active` | Validates CLI components used by SDK |
| Orbit CLI           | `active` | Validates CLI user-facing behavior   |
